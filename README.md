# SH Running Coach — v6.11 (멀티월·9월 내장·근력 통합 · 다크모드 · 훈련부하 · 세션 타이머)

비행 로스터 기반 개인 러닝·영양 코치 PWA. **로스터 JSON → 규칙 엔진으로 훈련·영양·근력 플랜 자동 생성.**

## 동작 (P0 + P2)
- **오늘 / 주간 / 계산기 / 설정** 4개 탭 (하단 탭바)
- **규칙 엔진** — 로스터의 근무 유형(off·flight·layover·reserve)과 플래그(sectors·nightDep·redeye·early·coast·humid·heat·hint)로 세션·탄수·시간대·야외/실내·저녁/걷기 옵션을 **자동 도출**
- **프로필 캘리브레이션** — AT/AnT·페이스 바꾸면 러닝머신 속도·시간·존이 전 화면 자동 재계산
- **로스터 편집** — 설정 탭에서 JSON 붙여넣고 "적용 & 재생성" → 즉시 플랜 반영
- **계산기** — 페이스↔km/h, 거리(100/300/400/600m·1km)→시간
- 로컬 저장(localStorage) · 오프라인(service worker `shrc-v4`) · 홈 설치(manifest)


## Phase 3 · 기록 / 자동 보정 (신규)
- **기록 탭** — 체중 입력 → 추세 그래프·로드맵·주간 감량률·조정 제안(정체 시 −kcal / 과속 시 +kcal)
- **러닝 로그** — 날짜·종류·거리·시간·평균HR·최대HR·케이던스·kcal 입력·저장(localStorage `shrc_logs`)
- **자동 보정** — 최근 이지런 평균 HR을 AT와 비교 → "너무 세게" 경고 / "존2 잘 지킴", 관측 최대심박 > 설정이면 상향 제안
- **날씨 연동** — 설정 탭 "날씨 불러오기(수원)" → Open-Meteo(무키·CORS) 최고기온으로 로스터 `heat` 자동 채움 → 플랜 재생성 (오프라인이면 조용히 실패, 수동 heat 유지)
- **아이콘** — `icon-192.png`·`icon-512.png` 포함 (설치 아이콘 적용)


## v4 신규 (다크모드 + 상용앱 벤치마크)
- **다크 테마** — 전체 UI 다크(네이비/틸). 기존 PWA들과 톤 통일.
- **세션 타이머** (오늘 탭) — 구조화된 세션을 단계별로 자동 진행. 인터벌이면 웜업 10분 → 스트라이드 3회(각 28초+걷기 45초) → 1km 반복 ×N(사이 걷기 90초) → 쿨다운. 단계마다 **목표 러닝머신 속도** 표시, 단계 종료 시 **진동 알림**. 러닝머신에서 폰 보고 그대로 따라가면 됨.
- **훈련 부하 (급성/만성 비율)** — Garmin ACWR 방식. 급성(7일)/만성(28일 주평균)/비율 + 상태 판정
  - 0.8 미만 회복·디트레이닝 / **0.8~1.3 최적** / 1.3~1.5 과부하 주의 / **1.5↑ 부상 위험**
  - 세션별 부하는 평균HR(HR reserve)·시간·세션 종류 가중(TRIMP 근사)으로 산출
- **강도 분포 80/20** — 최근 28일 이지 vs 고강도 시간 비율. 이지 75% 미만이면 "더 느리게" 경고 (이지런 과속 습관 교정용)
- **주간 요약** — 최근 7일 거리·시간·세션 수·소모 kcal

## v6.11 — 스텝 빌더 (인터벌 타이머 앱 구조 흡수)
- **트레드밀 루틴 블록** — 모든 세션을 `[타입 × 속도(km/h) × 시간]` 블록 + **인터벌 그룹(N회 반복)** 구조로 자동 생성·표시. 프로필 페이스 → **트레드밀 0.5단위 속도**로 변환(인터벌 10.0 / 템포 9.0 / 이지 8.0·7.5 / 걷기 5.0 등 실제 세팅 그대로)
- **자동 요약** — 총 소요시간 · 예상거리 · 예상 kcal (속도×시간 합산, 체중 기반)
- **편집 모드** — 반복 ±, 속도 ±0.5, 시간 ±30초, 블록 추가(걷기/달리기/전력질주)·삭제, 그룹 추가·삭제. 날짜별 저장(`shrc_routines`), **초기화**로 자동값 복귀
- **내 루틴 라이브러리** — 현재 루틴을 이름 붙여 저장, 다른 날에 불러오기 (`shrc_lib`)
- **복사** — 타이머 앱 입력 형식 텍스트(`[5회] 달리기 10.0km/h 6:00 → 걷기 5.0km/h 1:30`)로 클립보드 복사
- **내장 타이머가 루틴 사용** — 편집한 블록 그대로 반복 전개해 실행 (속도·페이스 병기)

## v6.10 — 일정 변경 버그 수정
- **세션 변경이 모든 근무일에 적용** — 이전엔 flight/layover 날에서 타입을 바꿔도 엔진이 kind에서 세션을 자동 생성해 무시됐음. `force` 오버라이드 추가로 **비행일이든 예비일이든 강제 적용**
- **↺ 자동 배치로 되돌리기** 버튼 추가 (수동 지정 해제) · "내일로 미루기"도 force 방식으로 동일 적용

## v6.9 — 세션 완료·기록 + 일정 변경 + UI 가독성
- **세션 완료/건너뜀/되돌리기** — 오늘 탭에서 하루 상태 관리. 완료 시 거리·시간·HR·케이던스·RPE 간단 기록 → 훈련부하·80/20·주간요약·로그에 자동 반영
- **일정 변경** — 세션 타입 바꾸기(이지·회복·템포·인터벌·롱런·휴식) + "내일로 미루기". 로스터에 저장돼 유지, 근력·규칙엔진 자동 재계산
- **주간 탭 완료 표시** — 완료/건너뜀 마커
- **UI 가독성 개선** — 날짜 헤더 확대, 상태 배너, 액션바, 메인 지표 강조, 세션 변경 picker, 정돈된 간격·색상

## v6.8 — 속도·페이스 병기 + 총시간 전체합산
- **모든 구간에 페이스·속도 동시 표기** — 메인/웜업/스트라이드/회복/쿨다운, 타이머 스텝까지 `6\'00~6\'10 · 9.7~10.0 km/h` 형식. 걷기·스트라이드처럼 km/h만 있던 값에도 페이스 병기
- **총 운동시간 = 전체 합산** — 메타의 "총 약 X분"이 웜업+스트라이드+메인+회복+쿨다운을 모두 포함 (예: 인터벌 5본 60분, 이지 5km 51분, 임계 43분, 롱런 93~116분)

## v6.7 — 페이스/속도 깨짐 수정
- **인터벌 등 페이스가 0\'00 / Infinity km/h로 뜨던 버그 수정** — 저장된 프로필의 페이스 값이 0·NaN·누락이면 로드 시 기본값으로 자동 복구(`loadProfile` 검증). 사용자가 직접 설정한 유효값은 보존
- 저장 시에도 빈/잘못된 페이스 칸은 기존값 유지(재오염 방지), `kmh()`에 0 방어 추가

## v6.6 — 오늘 탭 하루 넘기기
- **오늘 페이지 좌우 스와이프**로 하루씩 이동 (◀ 이전날 / 다음날 ▶). 화살표 버튼(‹ ›)·"↩ 오늘로" 버튼도 추가
- 월 경계에서 자동 롤오버 (8/31 → 9/1, 9/1 → 8/31). 세로 스크롤·작은 움직임은 무시
- 오늘이 아닌 날은 "오늘 ·" 라벨 없이 날짜만 표시 + 오늘로 복귀 버튼 노출

## v6.5 — 8월 실제 스케줄 반영
- 내장 8월 로스터를 **실제 크루 스케줄과 정확히 일치**하도록 교정 (라벨·섹터·레이오버·야간/레드아이)
- **22일 수정**: 야간 레드아이(오인) → 실제 **OFF**(21일 ICN-NRT 야간 왕복 00:25 종료 후 회복일). 잔여일(26~31) 라벨 정확도 개선
- 시드 v2로 올려 기존 저장분에 자동 반영

## v6.4 — 멀티월 (8월+9월 동시)
- **여러 달 동시 보관·탐색** — 8월·9월 둘 다 내장. 오늘/주간 탭 상단 `‹ 2026-08 ›` 월 네비로 전환
- **기본은 이번달** — 실제 현재 월로 열림(없으면 최근 달). 다른 달은 "미리보기"로 표시(오늘 하이라이트 없음)
- **JSON = 월 추가/갱신** — `{"month":"2026-10",...}` 붙여넣고 적용하면 그 달이 추가되고 이동. 기존 달 JSON이면 그 달만 갱신
- **저장 구조 변경** — `shrc_rosters`(월→로스터 맵). v6.3 이하의 단일 로스터·시드는 자동 마이그레이션 후 8월+9월 기본값 복원
- 달마다 규칙 엔진·근력 자동배치 독립 실행 (8월: 17A/18B/24A · 9월: 4·10·17·26=A, 13·27=B)

## v6.3 — 9월 로스터 내장 + 월 자동화
- **9월(2026-09) 로스터를 기본값으로 내장** — JSON 붙여넣기 없이 앱만 업데이트하면 9월 플랜이 바로 뜸
- **날짜 로직 월 인식화** — 요일·주 구조(일~토)·"오늘" 날짜를 `ROSTER.month`에서 자동 계산 (더는 8월 하드코딩 아님, `buildWeeks()`)
- **시드 자동 적용** — `ROSTER_SEED` 변경 시 저장된 옛 로스터를 새 기본값으로 자동 교체. 이후 달은 DEFAULT_ROSTER 교체 + 시드만 올리면 전 기기 자동 반영
- 9월 플랜: 롱런 5·16·25 / 인터벌 11·20 / 회복 4·10·26 / 근력 A×4(4·10·17·26) B×2(13·27) · 예비일(SBY/RESV/TRAIN) 자동 easy

## v6.2 보정
- **근력 시작 게이트 월 인식화** — `STR_START='2026-08-17'`로 변경. 시작 월(8월)엔 17일부터, **이후 달(9월~)은 전체 허용**. (v6.1은 날짜 숫자만 비교해 9월 1~16일 근력이 막히던 버그 수정)

## v6.1 보정 (실측 데이터 반영)
- **근력 시작 8/17로 이동** — `STR_START_DAY` 게이트 추가, 그 전 자동배치 안 함
- **트레드밀 이지 실내보정** — 실내는 심박 5~10 높게 나와, 상한을 ≈8.0km/h로 캡(8.3 금지)하고 **심박 ≤AT-1 우선**·걷기 5분 워밍업으로 조정 (8/12 트레드밀서 8.3km/h에 심박 145 뜬 것 반영)
- **케이던스 "러닝 구간 기준" 명시** — 걷기 섞이면 평균 깎이므로 러닝 구간으로 판단

## v6 신규 · 근력(플랜핏) 통합
- **주 2회 자동 배치** — 확정된 플랜핏 커스텀 플랜을 오늘/주간에 자동 표시
  - **Day A · 상체 우세(30분, 플랜핏 플랜 1)**: 트위스트슈퍼맨·와이드푸쉬업·에어레터럴·와이드스쿼트 2세트·카프·사이드플랭크
  - **Day B · 하체 우세(40분, 플랜핏 플랜 2)**: 밴드글루트브릿지·사이드킥백·레그컬·카프·와이드푸쉬업·데드벅
- **배치 규칙 엔진** — 홈데이(off/reserve)에만. **러닝 먼저 → 근력.**
  - **B(하체)**: 전날·다음날·당일 퀄리티(인터벌/임계)·롱런 없는 날에만 (다리 보호)
  - **A(상체)**: 배치 자유(롱런일 제외, 이지·휴식일 우선)
  - 홈데이 부족한 주는 자동 1회로 축소
- **수동 override** — 로스터 일별 `"strength":"A"|"B"|"skip"` → 자동배치보다 우선
- **훈련 부하 반영** — 근력 로그(type `strength`)는 ACWR에 약하게(×0.6) 반영, 러닝 80/20 분포에선 제외
- 종목/세트는 실제 운동은 **플랜핏 앱**에서 수행, 본 앱은 *어느 날 A/B를 할지* 배치·규칙만 관리

## 참고한 상용앱 개념
- Garmin: Acute/Chronic Training Load, Load Ratio(1.5↑ 부상위험), Training Status
- Strava: Relative Effort(3주 롤링 평균 대비 주간 부하 밴드)
- 폴라라이즈드 트레이닝: 80/20 강도 분포

## 로스터 스키마
```json
{ "month":"2026-08", "days":[
  { "d":7, "kind":"off", "label":"OFF", "hint":"interval", "reps":4, "heat":"낮 37°C 폭염peak" },
  { "d":8, "kind":"flight", "label":"FLT ×3", "sectors":3, "layover":"부산" },
  { "d":10,"kind":"layover","label":"L/O 홍콩","humid":1,"heat":"홍콩 32°C" },
  { "d":18,"kind":"reserve","label":"RESV","hint":"easyrec" }
]}
```
- `kind`: off · flight · layover · reserve
- `hint`(off/reserve): interval · threshold · long · easy · easyrec · rest  (없으면 easy)
- 플래그: `sectors` · `nightDep`(야간 출발지) · `redeye` · `early`(다음날 이른 show-up) · `coast`(해안 레이오버) · `humid` · `heat` · `opt`(선택 세션) · `reps`(인터벌 반복수) · `reason`(휴식 사유) · `strength`("A"·"B"·"skip" 근력 수동 지정)

## 규칙 요약 (engine)
- **세션**: off→hint / layover(야간출발)→해안이면 이지·아니면 트레드밀 / layover(종일)→트레드밀 / flight(야간출발)→오전 이지 / flight(레드아이·5섹터·3섹터)→휴식
- **탄수**: 퀄리티·롱런·3섹터·야간=고탄수 / 그 외=저탄수 / 레드아이=회복
- **저녁 옵션**: 다음날 early·redeye·5섹터면 퀄리티 저녁 금지
- **걷기 옵션**: 단순 왕복·레이오버行(레드아이·이른근무·5섹터·야간출발 제외)

## 배포 (github.io)
`index.html`·`manifest.json`·`sw.js` push → Settings→Pages→브랜치 → 폰에서 "홈 화면에 추가".
> 업데이트 시 `sw.js`의 `CACHE`(현재 `shrc-v17`)를 올려야 새 버전이 강제 적용됨.

## 다음 (Phase 3~)
- 운동 로그 입력 → 존·페이스 자동 보정, 체중 추세·조정 제안
- 날씨 API(Cloudflare Worker 프록시) → heat 자동 채움
- Google Drive 백업(기존 로직) · SH Pilot Logbook 로스터 공유

> 코칭 로직은 실측 기반 참고용이며 의료 자문이 아님.
